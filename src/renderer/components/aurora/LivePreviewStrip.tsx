import React from 'react';

// Stub — real implementation lands in commit 7. Keeping it as a sized
// placeholder so the hero layout doesn't shift when the strip ships.
export const LivePreviewStrip = (): React.JSX.Element => (
  <div className="h-[180px] w-full" aria-hidden="true" />
);
