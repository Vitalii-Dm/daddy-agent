import React from 'react';

// Placeholder — replaced in commit 8.
export const DashboardSection = (): React.JSX.Element => (
  <section
    id="dashboard"
    className="relative min-h-screen px-8 py-24 text-[color:var(--ink-1)]"
    style={{ scrollMarginTop: '88px' }}
  >
    <div className="mx-auto max-w-7xl text-center font-mono text-[12px] uppercase tracking-[0.18em] text-[color:var(--ink-3)]">
      Dashboard — coming up
    </div>
  </section>
);
